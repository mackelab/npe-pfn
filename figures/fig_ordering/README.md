# Ordering Figure

## Preview

![Ordering Figure](fig_ordering.svg)

## Results Sources

- `main_results/ordering/summary.csv`

## Experiments

- `conf/experiment/order_tabpfn.yaml`
- `conf/experiment/permuted_sbibm_tabpfn.yaml`

## Notes

- The notebook in this folder reads `main_results/ordering`.
- That summary contains both the handcrafted ordering tasks (`simple_nonlinear`, `mixed_dist`) and the SBIBM permutation runs (`permuted_task`).
