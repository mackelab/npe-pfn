name: cnn
params:
  in_channels: 1
  out_channels_per_layer: [16,32,64,128]
  num_conv_layers: 4
  num_linear_layers: 2
  num_linear_units: 100
  output_dim: 10
  kernel_size: 3
  pool_kernel_size: 2
