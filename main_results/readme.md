# Main paper results

This is a collection of the main paper results, which are used to create the figures.
Note that this collection does not include metadata i.e. saved models, just the final metrics + configs!

New results will that was generated by running `tabpfn_sbi` is by default placed in `results/`.
